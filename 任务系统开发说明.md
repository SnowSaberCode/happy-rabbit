# 快乐兔园 - 任务系统开发说明

## 功能概述
为游戏添加了完整的任务系统，包括每日任务、成就任务和进度任务，为玩家提供明确的目标和奖励机制。

## 新增文件

### 1. `scripts/task_manager.gd` (核心管理器)
- **功能**：全局单例，管理任务定义、进度跟踪、奖励发放
- **任务类型**：
  - 每日任务：每天重置，随机选择3-5个任务
  - 成就任务：一次性里程碑目标
  - 进度任务：长期累计目标

- **核心方法**：
  - `update_task_progress(category, amount)` - 更新任务进度
  - `increment_feed_count()` - 喂食次数+1
  - `increment_pet_count()` - 抚摸次数+1
  - `increment_shave_count()` - 剃毛次数+1
  - `increment_breed_count()` - 养兔子+1
  - `increment_drink_count()` - 喂水次数+1
  - `update_coins_earned(amount)` - 累计获得金币
  - `claim_reward(task_id)` - 领取任务奖励
  - `check_daily_reset()` - 检查每日任务重置

### 2. `scenes/task_panel.tscn` (任务面板场景)
- **功能**：任务系统UI主面板
- **组件**：
  - 顶部标题栏和关闭按钮
  - 三个标签页：每日任务、成就、进度
  - 滚动容器显示任务列表

### 3. `scripts/task_panel.gd` (任务面板脚本)
- **功能**：任务面板UI逻辑
- **特性**：
  - 标签页切换
  - 动态渲染任务项
  - 响应任务进度更新
  - 领取奖励交互

### 4. `scenes/task_item.tscn` (任务项场景)
- **功能**：单个任务的UI显示组件
- **显示内容**：
  - 任务图标和名称
  - 任务描述
  - 进度条和进度数值
  - 奖励信息
  - 领取按钮（完成后可用）

### 5. `scripts/task_item.gd` (任务项脚本)
- **功能**：单个任务项的UI逻辑
- **特性**：动态更新任务状态、响应领取按钮点击

## 修改文件

### 1. `project.godot`
- 添加 TaskManager 为 Autoload 单例

### 2. `scripts/game_manager.gd`
- `add_coins()` 中添加金币累计任务更新
- `spawn_rabbit()` 和 `spawn_rabbit_from_data()` 中添加养兔子任务更新

### 3. `scripts/main.gd`
- 添加任务面板节点引用
- 添加 `_on_task_button_pressed()` - 打开任务面板
- 添加 `_on_task_panel_close()` - 关闭任务面板
- 在喂食、喂水、抚摸、剃毛成功后调用任务进度更新
- 在 `_ready()` 中检查每日任务重置
- 在点击空白区域判断中添加任务面板判断

### 4. `scripts/save_manager.gd`
- 保存/加载任务系统数据（任务进度、每日任务状态、累计统计）
- 创建新存档时重置任务系统

### 5. `scenes/main.tscn`
- 在顶栏添加"📋 任务"按钮
- 实例化任务面板场景
- 连接按钮信号

## 任务列表

### 每日任务 (5个，每天随机3-5个)
1. 喂食小能手 - 喂食5次，奖励50金币
2. 亲密互动 - 抚摸10次，奖励80金币
3. 剃毛达人 - 剃毛3次，奖励100金币
4. 今日小赚 - 获得500金币，奖励100金币
5. 及时补水 - 喂水3次，奖励30金币

### 成就任务 (5个)
1. 初来乍到 - 拥有第一只兔子，奖励100金币
2. 小有积蓄 - 累计获得1000金币，奖励200金币
3. 小富翁 - 累计获得10000金币，奖励2000金币+胡萝卜蛋糕x3
4. 兔子大家庭 - 同时养5只兔子，奖励500金币
5. 熟练理发师 - 累计剃毛10次，奖励300金币

### 进度任务 (3个)
1. 资深饲养员 - 累计喂食100次，奖励500金币
2. 最受欢迎铲屎官 - 累计抚摸200次，奖励800金币
3. 剃毛大师 - 累计剃毛50次，奖励1000金币
4. 大富翁 - 累计获得50000金币，奖励5000金币

## 技术特性

1. **信号驱动**：任务进度更新通过信号通知UI实时刷新
2. **数据持久化**：所有任务进度、状态自动保存到存档
3. **每日重置**：基于系统日期自动重置每日任务
4. **模块化设计**：任务系统与其他系统解耦，易于扩展
5. **与现有系统无缝集成**：金币、背包、存档系统完美对接

## 使用说明

1. 点击顶栏"📋 任务"按钮打开任务面板
2. 查看当前进行中的任务和进度
3. 任务完成后，点击"领取奖励"按钮获得奖励
4. 每日任务每天自动重置，记得每天完成哦！

## 后续扩展建议

1. 添加更多任务类型（物品收集、设施建造等）
2. 添加任务完成时的特效和音效
3. 添加每周任务/活动任务
4. 添加任务排行榜（本地/云端）
